package controller;

public class LerPoluicao {
	private Integer posicao;
    private String  pais;
    private String  qtdePoluicao;

    // Gets e Sets omitidos para ficar menor

    public String toString() {
        return "{posicao='" + posicao + "\', pais=" + pais + ", qtdePoluicao='" + qtdePoluicao + "\'}";
}
}
